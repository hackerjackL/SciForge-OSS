# SciForge-OSS v3.0 优化方案（修正版）

> **状态**: 诊断 + 方案，未动任何代码。等你拍板后我才动手。
> **修正说明**: 上一版方案存在概念偏差——把"工程落地"理解成了人类工程团队的项目落地。实际 OSS 的工程落地是 **AI 写代码、跑实验、辅助科研**，核心是 AI for Scientist Anything，不是商业项目，不是人类工程团队。

---

## 0. 对你的纠正做系统性消化

| 上一版我理解错了什么 | 正确的理解 | 对方案的影响 |
|---------------------|-----------|-------------|
| G1 Temporal Maturity 按"人类产业成熟度"评估 | 按 **AI 开发周期**评估——AI agent 从 idea 到跑出代码/实验需要多少轮交互、多少 token、多少 compute | 评估标准从"产业有没有条件"改为"AI 有没有能力" |
| G2 Talent Availability（人才可用度） | **删除**——AI 替代人类，不存在"人才短缺"问题 | 去掉这个维度 |
| G5 Economic Sustainability（经济可持续性） | **删除**——OSS 不做商业项目，不评估"维护成本" | 去掉这个维度 |
| EG 轴是"人类工程团队能不能落地" | EG 轴是 **"AI agent 能不能把这个 idea 写成代码、跑出实验"**——写代码、跑实验、辅助科研，不限领域 | 重新校准 EG 子维的评分标准 |
| 出图用 SVG | **用 PDF**——矢量图质量更高 | 输出格式改为 PDF |
| 落地路线 = 时间轴图 | 落地路线 = **AI 开发路线**——AI agent 分阶段写代码、跑实验、验证 trick | 重新设计 Engineering Path 的内容 |

---

## 第一部分：pipeline 断裂诊断（6 处）

上一轮体检发现的 6 处断裂依然有效——pipeline 产物流转、phase mode table、归档契约、ASCII 陈旧计数——这些与 EG 轴的概念无关，是纯文档/契约问题。

| # | 位置 | 问题 | 严重度 |
|---|------|------|--------|
| **F1** | auto-pipeline 行 86、97 | ASCII 图里写 "5-axis idea-fit"，但已升 6-axis | 低（文档） |
| **F2** | auto-pipeline 行 127 | ASCII 图里 Phase 2.5 写 "5 维度攻击"，但已升 6 维 | 低（文档） |
| **F3** | auto-pipeline 行 132-133 | Phase 3 写 "3 维评估 + 旧公式"，但已升 4 维 | 低（文档） |
| **F4** | auto-pipeline Phase Mode Table | 缺 Phase 2.5b 行（EG 门控没进 mode 表） | 中（契约） |
| **F5** | auto-pipeline 最终组装 workspace | 缺 `ENGINEERING_GROUNDING.md` 的归档位置 | 中（契约） |
| **F6** | auto-pipeline Domain Signature 图 | 没列 Phase 2.5b 是否消费领域签名 | 低（设计） |

---

## 第二部分：EG 轴修正——从 AI 视角重写

### 2.1 核心原则重述

**EG 轴不是评估"人类工程团队能不能建"，而是评估"AI agent 能不能把这个 idea 写成代码、跑出实验"**。OSS 是 AI for Scientist Anything：
- 理工科 → AI 写代码、跑数值实验、验证理论推导
- 人文 → 可能 AI 写代码做文本分析/统计，也可能不需要代码
- 全领域适用，但"落地"的主体是 **AI agent 自己**，不是人类团队

### 2.2 EG 子维度修正（6 维 → 8 维）

去掉 G2 Talent Availability 和 G5 Economic Sustainability，保留 G1 但改定义，新增一个 AI-specific 维度。

| # | 子维 | 原定义（错误） | 修正定义（AI 视角） | 评分标准 |
|---|------|--------------|-------------------|---------|
| 1 | **Compute Footprint** | 人类团队需要多少 GPU-h | **AI agent 写代码、跑实验需要多少 compute** | 0=超出 OSS 沙盒预算 10 倍 / 5=1-10 倍 / 10=预算内 |
| 2 | **Dependency Chain** | 外部依赖未就绪 | **AI agent 能否自动获取/安装依赖**（数据集、库、工具、仪器驱动） | 0=≥3 个依赖 AI 无法自动获取 / 5=需辅助 / 10=全自动可用 |
| 3 | **AI Dev Cycle**（←原 Team-Year） | 人类的人月估算 | **AI agent 从 idea 到跑出可复现结果需要的交互轮次 + token 量级** | 0=> 24 轮交互（1 个完整 pipeline 跑 24 次） / 5=6-24 轮 / 10=≤6 轮 |
| 4 | **Reproducibility Risk** | 人类团队 trick 假的风险 | **AI agent 的核心 trick 假后需要重写多少代码** | 0=> 50% 概率 trick 假 → 全盘重写 / 5=20-50% / 10=< 20% |
| 5 | **Capital Cost** | 非算力资本投入 | **AI 是否需要采购专有数据、付费 API、设备** | 0=需新基建/新采购 / 5=需采购但市售 / 10=仅用现有资源 |
| 6 | **Code Complexity**（新增） | — | **AI agent 实现这个 idea 的代码复杂度**——代码量、深度、是否涉及多模块协同 | 0=需写 > 5000 行代码或跨 5+ 模块 / 5=1000-5000 行 / 10=≤1000 行 |
| 7 | **Temporal Maturity**（改定义） | 产业成熟度 | **AI agent 当前能否实现这个 idea**——依赖的技术（如某类 LLM 能力、某类工具链）当前是否就绪 | 0=当前 AI 能力完全做不到 / 5=需等 3-6 个月工具链成熟 / 10=现在就能做 |
| 8 | **Regulatory Readiness**（保留） | 监管/法律 | **idea 涉及的数据合规、伦理审批、出口管制是否影响 AI 执行** | 0=监管明文禁止 / 5=灰色地带 / 10=无限制 |

### 2.3 AI Dev Cycle 的详细评分标准

这个维度替代了原来的 Team-Year Estimate，是**最关键的修正**——不再用"人月"，而用 AI 的交互轮次：

| 分数 | AI 交互轮次 | 对应场景 | 例子 |
|------|-----------|---------|------|
| 0 | > 24 轮 | AI 需要跑完整 pipeline 24 次以上才能收敛到一个可复现结果 | 需要反复调参、反复修 BUG、反复换工具链 |
| 3 | 13-24 轮 | AI 需要大量调试迭代 | 代码量大、依赖链长、频繁报错 |
| 5 | 6-12 轮 | AI 需要中等量级的调试 | 核心代码 1-2 次写对，但实验验证需多次 |
| 8 | 3-5 轮 | AI 快速实现 | 核心 trick 简单，代码量小 |
| 10 | ≤ 2 轮 | AI 几乎一次写对 | 极简 idea，纯理论推导无需代码 |

**为什么不估 token 而估轮次**：token 是 LLM 的输出，但"落地"的核心是**AI 写代码→跑→报错→修→再跑**这个循环的次数。轮次更直接反映"这个 idea 落地有多折腾"。

### 2.4 BLOCKED 规则修正

**旧规则**：任一子维 = 0 → BLOCKED
**新规则**：任意 **3 个**及以上子维 = 0 → BLOCKED

修正理由：你举的例子"1 年研发期"在 AI 视角下可能只是 AI Dev Cycle = 3（6-12 轮）。但一个 idea 同时满足"AI Dev Cycle = 0 + Compute Footprint = 0 + Dependency = 0"才真有可能是 AI 当前完全做不到——3 个门槛才砍。

### 2.5 Extreme Idea Protocol（新增）

单子维 = 0 或 2 个子维 = 0 的 idea → **不淘汰**，进入 MCTS 但标 `ai_extreme`。EG 报告强制产出 **AI Mitigation Plan**：

```markdown
### AI Mitigation Plan（AI 视角的缓解方案）
- **Extreme sub-dimension**: AI Dev Cycle = 0（> 24 轮交互）
- **Mitigation option A**: 拆 idea 为 3 个子任务，每个 ≤ 8 轮，可串行
- **Mitigation option B**: 等工具链成熟（Temporal Maturity = 5，3 个月后有新工具）
- **Mitigation option C**: 用 simpler proxy 替代（降低 Code Complexity 评分）
- **Recommendation**: Option A 最可行——拆后 AI Dev Cycle 从 > 24 轮降至 8 轮，EG 子维升 5 分
```

这让你在 Phase 3→4 强制审批时看到"这个 idea 极端但可缓解"，决策权在你手上。

---

## 第三部分：agent/skill 边界审阅——当前约束不严谨处

我把整个 OSS 的 skill 约束 + agent 自由度审了一遍，找到 4 处真问题：

| # | 位置 | 问题 | 严重度 |
|---|------|------|--------|
| **B1** | auto-pipeline 的 Parallelization 表（行 74-79） | 说 Phase 2 + Phase 4 可并行，但 Phase 2 的 idea-discovery 需要 Phase 4 的文献结果来评估 novelty——并行可能冲突。**约束不严谨** | 中 |
| **B2** | auto-pipeline 的 Early Exit 表（行 97） | Phase 3 得分 > 0.8 时跳过 Phase 2.5（adversarial falsification）。**但得分 > 0.8 不代表 trick 真**——跳过证伪门控是危险的。**约束太松** | 高 |
| **B3** | idea-discovery 的 MCTS Round 2（行 86-89） | 说 borderline 才重评，clear PASS 和 clear FAIL 不重评。**但 clear PASS 的 idea 可能只是"看起来好"**——与 B2 的早期退出相同问题。**约束太松** | 中 |
| **B4** | adversarial-falsification 的 Phase 5b 执行条件 | 说"Phase 5a BLOCKED 则跳过 Phase 5b"——但 Phase 5a（OSS sandbox 可行性）和 Phase 5b（AI 工程落地）是两回事。沙盒不可行不代表工程不可行。**约束不正确** | 高 |

### 修正方向

| # | 修正 |
|---|------|
| B1 | 移除并行声明，或改为"Phase 2 的 novelty 评估依赖 Phase 4 文献；但如果 Phase 2 先跑 MCTS 前几轮、Phase 4 并行跑文献，第 4 轮再合并是可接受的"——加条件约束 |
| B2 | 移除"得分 > 0.8 跳过 Phase 2.5"这条。**证伪门控是强制性的，不可跳过** |
| B3 | 改为"clear PASS 的 idea 在 MCTS 第 2 轮只做轻量级 re-score（不重跑完整评估），但第 3 轮仍必须参与竞争"——加轻量级约束 |
| B4 | 移除"Phase 5a BLOCKED 则跳过 Phase 5b"。改为"Phase 5a BLOCKED → Phase 5b 仍必须执行，但 Note 标注 'sandbox blocked'——AI 工程落地独立评估" |

---

## 第四部分：F1-F6 pipeline 断裂修复

| # | 文件 | 改动 |
|---|------|------|
| F1-F3 | `auto-pipeline/SKILL.md` | ASCII 图里 3 处陈旧计数更新 |
| F4 | `auto-pipeline/SKILL.md` | Phase Mode Table 加 "2.5b: MUST" 行 |
| F5 | `auto-pipeline/SKILL.md` | 最终组装 workspace 加 `refine-logs/ENGINEERING_GROUNDING.md` |
| F6 | `auto-pipeline/SKILL.md` | Domain Signature 传播图加 Phase 2.5b 消费签名 |

---

## 第五部分：实施路线图（三轮递进）

### 轮次 A：pipeline 修裂 + agent/skill 边界修正（6 文件）

| # | 文件 | 改动 |
|---|------|------|
| 1 | `auto-pipeline/SKILL.md` | F1-F6 全部修复 + B1-B4 约束修正 |
| 2 | `idea-discovery/SKILL.md` | B3 修复（clear PASS 的 MCTS 重评规则） |
| 3 | `adversarial-falsification/SKILL.md` | B4 修复（Phase 5b 执行条件）+ EG 子维同步 |
| 4 | `engineering-grounding-contract.md` | 重写——去掉 G2/G5，G1 改 AI Dev Cycle，新增 Code Complexity + Temporal Maturity，BLOCKED 规则改 3 个，加 Extreme Protocol |
| 5 | `idea-dag-schema.md` | engineering_grounding 字段同步 8 维 |
| 6 | `novelty-check/SKILL.md` | 复合评分公式中 EG 权重不变，但 EG 归一化参考 8 维 |

### 轮次 B：EG 报告出 PDF 图 + 理论图增强（3 文件）

| # | 文件 | 改动 |
|---|------|------|
| 1 | `unified-plotting/SKILL.md` | 新增 `ai-dev-path` 图表类型——PDF 格式，显示 AI 开发路线（Stage 1/2/3 每次交互的代码量 + 预期报错点 + 缓解策略） |
| 2 | `engineering-grounding-contract.md` | 声明 EG 报告 Phase 5b 完成后自动调用 unified-plotting 产出 `ENGINEERING_GROUNDING_PATH.pdf` |
| 3 | `adversarial-falsification/SKILL.md` | Phase 5b 模板增加 PDF 图引用 |

### 轮次 C：全领域通用性增强 + 125 问题 demo 对齐（4 文件）

| # | 文件 | 改动 |
|---|------|------|
| 1 | `domain-adaptive-pipeline.md` | 加 humanities/interpretive 领域 EG 子维的 N/A 规则（Compute Footprint = N/A、Code Complexity = 可能 N/A） |
| 2 | `engineering-grounding-contract.md` | 全领域适用表——理工科 8 维全填，人文 Code Complexity 可能 N/A |
| 3 | `125-SCIENCE-PROBLEMS.md` | 125 问题 demo 的 EG 评分参考（每个问题标注 EG 8 维预估） |
| 4 | `README.md` | 更新 EG 描述为 AI 视角 |

---

## 第六部分：请你拍板（5 个问题）

| # | 问题 | 我的建议 |
|---|------|---------|
| 1 | EG 8 维（去掉 G2/G5，G1 改 AI Dev Cycle，新增 Code Complexity + Temporal Maturity）是否接受？ | **接受**——这 8 维从 AI 视角覆盖了"写代码、跑实验、验证 trick"的全链路 |
| 2 | BLOCKED 规则从"任一子维=0"改为"任意 3 个及以上=0"是否接受？ | **接受**——单子维极值不该砍，3 个才真"AI 当前做不了" |
| 3 | Extreme Idea Protocol（单子维/2 子维=0 不砍，出 AI Mitigation Plan）是否做？ | **做**——这是"不简化创新"的核心保障 |
| 4 | 4 处 agent/skill 边界修正（B1 并行约束、B2 跳过证伪、B3 clear PASS 重评、B4 Phase 5b 执行条件）是否全做？ | **全做**——B2（跳过证伪）最危险，必须修 |
| 5 | 实施范围：轮次 A→B→C 三轮全做，还是先做 A 跑通？ | **三轮全做**——A 是修裂，B 是出图，C 是领域对齐，逻辑递进不冲突 |

**你拍板后我按 A→B→C 顺序闭环搞定**，每轮交付后跑 fast check 验证（0 坏链 + 0 非法 JSON + 0 phase 残留），最后推到 fork 分支。不动主仓库、不动 gewislab 分支、只推 fork。